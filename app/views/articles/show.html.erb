<html>
<head>
</head>
<%= @article.tags.each do |tag| %>
<%= link_to tag.name, tag_path(tag) %>
<% end %>
<body><h3>Comments (<%= @article.comments.size %>)</h3>
<p><%= @article.body %></p>
<%= render partial: 'articles/comment', collection: @article.comments %>
<%= render partial: 'comments/form' %>
<%= link_to "<< Back to Articles List", articles_path %> | <%= link_to "!! Delete Article", article_path(@article), method: :delete, data: {confirm: "Really delete this article?"} %> | <%= link_to "Edit", edit_article_path(@article) %>
</body>
</html>